let range1 = document.getElementById("range1");

range1.addEventListener("change", e => {
    // console.log(e)
    // e.target.value will be equal to the range's current value
});

/* these two functions will update based on x and y position of the xy controller */
function newXValue(value){
    // value has a range of 0 - 100
    //console.log(`xPos: ${value}`);
}

function newYValue(value){
    // value has a range of 0 - 100
    //console.log(`yPos: ${value}`);
}
